---
import { formatDate, readingTime, withBase } from '../lib/utils';
import { translations } from '../lib/i18n';

interface Props {
  title: string;
  description: string;
  category: string;
  tags: string[];
  date: Date;
  slug: string;
  body?: string;
}

const { title, description, category, tags, date, slug, body = '' } = Astro.props;
const base = import.meta.env.BASE_URL;
const url = (path: string) => withBase(path, base);
const catLabel = translations.zh.category[category as keyof typeof translations.zh.category]?.label || category;
const mins = readingTime(body);
---

<a href={url(`posts/${slug}/`)} class="post-link group block py-5">
  <div class="flex items-center gap-2 mb-1.5 text-[13px] text-[rgb(var(--text-3))]">
    <span class="font-medium text-[rgb(var(--text-2))]" data-i18n={`category.${category}.label`}>{catLabel}</span>
    <span>&middot;</span>
    <span>{formatDate(date)}</span>
    <span>&middot;</span>
    <span>{mins} min</span>
  </div>

  <div class="flex items-start justify-between gap-4">
    <div class="min-w-0">
      <h3 class="text-[16px] font-semibold leading-snug tracking-[-0.01em] text-[rgb(var(--text-1))] group-hover:text-[rgb(var(--accent))] transition-colors duration-150 mb-1">
        {title}
      </h3>
      <p class="text-[14px] text-[rgb(var(--text-3))] leading-relaxed line-clamp-1">
        {description}
      </p>
    </div>

    <!-- Arrow -->
    <svg class="w-4 h-4 mt-1 shrink-0 text-[rgb(var(--text-3))] opacity-0 -translate-x-1 group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-200" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
      <path d="M4.5 12h15m0 0l-6.75-6.75M19.5 12l-6.75 6.75"/>
    </svg>
  </div>
</a>

<style>
  .post-link {
    border-bottom: 1px solid rgb(var(--border));
  }
  .post-link:last-child {
    border-bottom: none;
  }
</style>
